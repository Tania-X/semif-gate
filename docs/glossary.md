# 术语地图：一次判定走完全程，每个名词落在哪一步

> 用**一次真实判定**把术语串起来。每个名词都标出「在流程的哪一步产生、谁产生的、拿来干什么」。
> 适合给不熟悉大模型但熟悉工程的人看。

---

## 全程总览

```
业务代码            网关                 判定服务              模型
   │                 │                     │                  │
   │ ① state         │                     │                  │
   ├────────────────►│                     │                  │
   │                 │ ② stateHash         │                  │
   │                 │ ③ 查缓存             │                  │
   │                 │ ④ POST /decide      │                  │
   │                 ├────────────────────►│                  │
   │                 │                     │ ⑤ tokenizer      │
   │                 │                     │   → token ids    │
   │                 │                     │ ⑥ 渲染 prompt    │
   │                 │                     │ ⑦ promptSha256   │
   │                 │                     ├─────────────────►│
   │                 │                     │                  │ ⑧ logits
   │                 │                     │◄─────────────────┤
   │                 │                     │ ⑨ 取 A/B/C 槽位
   │                 │                     │ ⑩ softmax        │
   │                 │◄── 分布 + 溯源 ──────┤                  │
   │                 │ ⑪ Decision 对象      │                  │
   │◄── 完整分布 ────┤ ⑫ 写缓存 + 审计      │                  │
   │                                                           │
   │ ⑬ 策略层：看 maxProbability / margin → 动作                │
```

---

## 逐步拆解：每一步是什么、为什么需要

### ① `state`（证据）— 业务方提供

```
"The optician ordered replacement lenses. The workshop confirms
 they have not yet been fitted to the customer's glasses."
```

**是什么**：要被判断的材料本身。
**谁产生**：业务方，运行时传入。
**为什么**：模型里没有任何业务知识。**换业务只换 state，不换模型。**

> ⚠️ state 越长，后面第 ⑧ 步越贵（注意力是 O(n²)）。
> 而且实测**塞无关内容会让判定变差**（翻转 11.1%），不只是变贵。

---

### ② `stateHash` — 网关算的

```
sha256(规范化后的 state)  → "cb418d9d9d9128af…"
```

**是什么**：state 的指纹。
**谁产生**：网关的 `StateNormalizer`（先按白名单裁剪、结构化截断，**再算哈希**）。
**为什么**：这是缓存键的第一个分量。**同一份材料我算过没有？**

> ⚠️ 顺序很重要：**必须先截断再算哈希**。反了的话，哈希描述的是一个从未被发送过的状态。

---

### ③ 缓存查找 — 网关做的

```
查找键 = sha256(stateHash + pointRef)
命中 → 直接返回，零模型调用（实测 0.07 ms）
未命中 → 继续往下走
```

**为什么**：同一份材料常被反复问。实测缓存命中带来 **1583×** 加速。

---

### ④ `pointRef` / 判定点 — 人工设计的冻结契约

```
"semif.bench.0cc61fdd0b37@1"
   │                      │
   │                      └─ version：改语义必须升版本（进缓存键）
   └─ 指向一份定义：判据 + 选项集 + 选项顺序 + model revision
```

**是什么**：把「要判断什么」变成**不可变契约**。
▸ **判据 question**：自然语言描述要回答什么
▸ **选项 options**：2–16 个候选，每个有语义 id + 描述
▸ **选项顺序**：⚠️ **它是语义的一部分**（见下方术语表）

**谁产生**：你/业务方。一旦冻结进版本控制，改动等同契约变更。

---

### ⑤ `tokenizer`（分词器）— 把文本变成整数

```
"The optician ordered…"  →  [785, 41834, 2266, …]     共 142 个整数
"A"                      →  [32]                       ← 答案槽位
"B"                      →  [33]
"C"                      →  [34]
```

**是什么**：文本 ↔ 整数数组的双向映射。**模型只认整数，不认字符串。**
**为什么重要**：**计费、长度限制、缓存都以 token 为单位，不是字符。**

> ⚠️ 关键约束（我们启动时会断言）：
> **答案字母必须是「恰好一个 token」且解码后往返一致。**
> 不满足就拒绝启动——否则读到的概率是错位的。

---

### ⑥ `prompt` — 真正发给模型的文本

```
<|im_start|>system
Apply the supplied criterion to the supplied evidence. …
<|im_start|>user
{"evidence": "…", "criterion": "…", "options": [{"letter": "A", …}]}
<|im_start|>assistant
<think>

</think>
```

**是什么**：由**冻结模板** + state + 判据 + 选项渲染而成。
**谁产生**：服务端，用注册表里的模板。

> ⚠️ **字母按选项下标分配**（`LETTERS[i]`）。
> 所以**选项顺序一变，字母↔选项的映射就变，prompt 就变**。

---

### ⑦ `promptSha256` — 证明「我们到底问了什么」

```
sha256(上面那段文本)  → "3cc9e3d1e4e07885…"
```

**谁产生**：服务端渲染后计算；调用方也独立算一遍，**两边必须一致，否则返回 409**。
**为什么**：这是审计链的根。判定可复现的前提是**输入完全相同**。

> 实测价值：它证明了我们这边渲染的 prompt 与对方**逐字节一致**，
> 从而把「结果不同」的排查范围**缩小到数值计算**，而不是模板问题。

---

### ⑧ `logits` — ⭐ 模型真正输出的东西

```
向量的维度 = 词表大小
一次性算出 248,044 个分数（每个 token 一个），我们只读其中 3 个
```

**这就是你问的那个词。** 详细解释见下一节。

---

### ⑨ 槽位读取 — 只取 3 个数

```python
vocabulary = logits[0, -1, :]      # 最后一个位置，整个词表
selected   = vocabulary[slots]     # 只取 A/B/C 三个槽位 → [21.875, 26.375, 24.375]
```

**为什么只读最后一位**：prompt 的最后一个位置，就是模型"准备回答"的位置。

---

### ⑩ `softmax` — 把分数变成和为 1 的概率

```
[21.875, 26.375, 24.375]  →  [0.009690, 0.872262, 0.118048]
```

详细解释见下一节。

---

### ⑪ `Decision` — 网关的返回对象

```jsonc
{
  "decisionId": "…",          // = 缓存键，确定性可复现
  "outcome": "OK",            // OK | DEGRADED
  "distribution": […],        // 完整分布（按 optionId 字典序）
  "provenance": {             // 溯源：模型版本、prompt 哈希、耗时…
    "modelRevision": "851bf6e8…",
    "promptSha256": "3cc9e3d1…",
    "inputTokens": 142,
    "latencyMs": 95
  }
}
```

**为什么返回完整分布而不是一个答案**：
实测有一条真实案例，`argmax` 给出的是**错的**，而正确答案就在并列位置上。
只返回一个值，就没有机会发现这件事。

---

### ⑫ 缓存写入 + 审计落盘

```jsonc
// 审计记录（每次判定一行）
{ "decisionId": "…", "stateHash": "…", "promptSha256": "…",
  "argmaxOption": "insufficient", "maxProbability": 0.474969,
  "margin": 0.0, "band": "REFUSE", "policyVersion": "v1",
  "cacheHit": false, "degradedReason": null }
```

**为什么**：让每一次判定事后都能回答「当时问了什么、模型说了什么、按哪版策略做的决定」。

---

### ⑬ 策略层 — 纯 Java，不碰模型

```java
if (!d.ok())                      → 保守动作
if (d.maxProbability() >= 0.90)   → 自动执行
if (d.maxProbability() >= 0.60)   → 人工复核
else                              → 保守动作
```

**为什么不在网关里做**：阈值和动作是**业务决策**，
随风险偏好、合规要求而变。塞进网关，网关就不可复用了。

---

## 术语深挖 1：`logits` 该怎么理解

### 定义

> **模型对词表里每一个 token 给出的「倾向分数」。它不是一个概率，只是一串没有界的实数。**

一次前向，模型在**每个位置**都输出一个长度 = 词表大小的向量。
我们只关心**最后一个位置**的那个向量，并且只读其中 3 个数。

### 真实数据

```
模型对这个案例给出：
  supported      = 21.875
  insufficient   = 26.375     ← 最高
  contradicted   = 24.375
```

### ⭐ 三个必须理解的性质

**性质 1：只有「差」有意义，绝对值毫无意义**

```
验证：
  原样       [21.875, 26.375, 24.375]  →  insufficient 概率 = 0.872262
  全部 −20   [ 1.875,  6.375,  4.375]  →  insufficient 概率 = 0.872262
  全部 +80   [101.875,106.375,104.375] →  insufficient 概率 = 0.872262
```

**完全相同。** 所以「logit = 26.375」这句话本身没有信息量，
有意义的是「**它比第二名高 2.0**」。

**性质 2：差距被指数放大**

```
logit 差 2.0  →  概率比 7.39 倍
logit 差 2.5  →  概率比 12.18 倍
logit 差 0    →  概率比 1（完全平局）
```

所以 logit 上一个很小的差异，在概率上会变成很大的差异。

**性质 3：它是「下一个 token 是谁」的分数**

logits 的原始含义是：**"如果现在要吐一个字，每个字各有多大倾向"**。
整个词表都参与（十几万维），我们只是**只读我们关心的那 3 个位置**。

### 为什么这个性质对我们重要

| 性质 | 带来的后果 |
|---|---|
| 只有差有意义 | 分布是**相对**的，不是绝对的把握 |
| 指数放大 | bf16 精度（24 附近间隔 0.094）会在接近平局时显著影响结果 |
| 只读 3 个槽位 | 若正确答案不在选项里，模型只能"摊派"给最不坏的那个 → **必须有弃权出口** |

---

## 术语深挖 2：`softmax` 该怎么理解

### 它做的事

> **把无界的分数（logits）压成一组和为 1 的概率。**

```
[21.875, 26.375, 24.375]  →  [0.009690, 0.872262, 0.118048]
```

### 手算（就 4 步）

```
① 找最大：max = 26.375        （减去它只为防溢出，不改变结果）
② 各自减 max：  −4.500, 0.000, −2.000
③ 取 exp：      0.0111, 1.0000, 0.1353
④ 除以和：      0.009690, 0.872262, 0.118048     （和 = 1）
```

Java 等价代码：

```java
double max = Arrays.stream(logits).max().getAsDouble();
double[] e = new double[logits.length];
double sum = 0;
for (int i = 0; i < logits.length; i++) { e[i] = Math.exp(logits[i] - max); sum += e[i]; }
for (int i = 0; i < e.length; i++) e[i] /= sum;
```

### ⭐ 三个必须理解的性质

**性质 1：相对性 —— 只关心差距**

```
insufficient(26.375) − contradicted(24.375) = 2.000  →  概率比 7.39 倍
```

**性质 2：条件性 —— 只在给定选项内成立**

```
给定 {supported, insufficient, contradicted} → 三者概率和恒为 1

如果换一组选项，所有概率都会变 —— 因为分母变了。
```

> **这就是为什么文档反复强调**：
> "Returned probabilities are **conditional on the supplied options**."
>
> `p = 0.95` **不代表**"这件事 95% 是对的"，
> 只代表"**在这几个候选里，它占 95% 的份量**"。

**性质 3：⚠️ 它不保证校准**

softmax 只保证「和为 1」，**不保证「0.9 意味着九成正确」**。

真实数据：

```
模型 softmax 后说 0.860   →   实际正确率 64.3%    差了 21.7 个百分点
```

**这就是 ECE 衡量的东西**——「声称的把握」与「实际的正确率」差多少。

---

## 术语速查表（按流程顺序）

| 术语 | 是什么 | 谁产生 | 用来干什么 |
|---|---|---|---|
| **state** | 被判断的证据 | 业务方 | 输入材料 |
| **stateHash** | state 的指纹 | 网关 | 缓存键 |
| **pointRef** | `判定点id@版本` | 人工冻结 | 指明问什么 |
| **question** | 判据（自然语言） | 人工冻结 | 告诉模型要回答什么 |
| **options** | 候选答案（含语义 id） | 人工冻结 | 限定答案空间 |
| **tokenizer** | 文本↔整数映射 | 模型自带 | 把文本变成模型能读的 |
| **token** | 一个整数 | tokenizer | 计费/长度/缓存的基本单位 |
| **prompt** | 实际发给模型的文本 | 服务端渲染 | 模型真正看到的输入 |
| **promptSha256** | prompt 的指纹 | 双方各算一遍 | 证明"问了什么" |
| **logits** | 模型给的原始分数 | **模型** | 未归一化的倾向 |
| **slot** | 答案字母对应的 token 位置 | 启动时自检 | 从十几万维里挑出要读的 |
| **softmax** | 分数→概率 | 服务端 | 变成和为 1 的分布 |
| **probability** | 归一化后的概率 | 服务端 | 输出的核心 |
| **maxProbability** | 最高的那个概率 | 网关 | 分档依据 |
| **margin** | 第一名 − 第二名 | 网关 | 判断"是否接近平局" |
| **ECE** | 声称 vs 实际的差距 | 评测阶段 | 判断"数字可不可信" |
| **temperature** | softmax 前的缩放系数 | 校准阶段拟合 | 修正过度自信 |
| **Decision** | 判定结果对象 | 网关 | 返回给调用方 |
| **band** | AUTO/REVIEW/REFUSE | 策略层 | 决定动作 |

---

## 一句话串起来

> 业务方给 **state**（证据），网关对它算 **stateHash** 并查**缓存**；
> 未命中就把 **pointRef**（冻结的判据+选项）发给判定服务；
> 服务用 **tokenizer** 把渲染好的 **prompt** 变成 token，算出 **promptSha256** 留证；
> 模型吐出 **logits**（词表大小的原始分数），我们只读 **A/B/C 三个槽位**；
> 经 **softmax** 变成概率分布；网关包成 **Decision**（带溯源）并落**审计**；
> 策略层看 **maxProbability** 与 **margin** 决定是自动执行、人工复核还是保守处理。
