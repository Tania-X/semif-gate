package com.semif.gate.contract;

/**
 * 判定所处的不确定性档位。
 *
 * <p>由策略层根据完整分布计算得出，网关本身不解释它的业务含义——
 * 网关只负责把档位标注在判定结果上，动作由调用方的策略表决定。
 */
public enum Band {
    /** 高置信度：可直接自动执行。 */
    AUTO,
    /** 中间带：需要人工复核。 */
    REVIEW,
    /** 低置信度或弃权：走保守默认动作。 */
    REFUSE
}
